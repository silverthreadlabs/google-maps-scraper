cache/
vendor
bin
webdata/
examples/examples-api/map-outputs
examples/examples-api/**/__pycache__/
examples/examples-api/**/*.py[cod]

# Orchestration + knowledge graph (local-only)
.lgtm/
graphify-out/

# Outreach pipeline (Python venv + caches)
outreach/.venv/
outreach/.env
outreach/**/__pycache__/
outreach/**/*.py[cod]
outreach/pipelines/*/enrichment/*.json
outreach/pipelines/*/enrichment/*/
outreach/pipelines/*/outputs/*/

# Outreach campaigns — track config (campaign.yaml, overrides.py, queries/)
# and deliverables (outputs/) only; heavy regenerable data stays local.
outreach/campaigns/*/raw/
outreach/campaigns/*/enrichment/

# Outreach docs — kept local, not tracked
outreach/docs/

# Outreach local analysis / simulation scratch
outreach/.reports/

# gosom scraper internal state (test runs from before our work)
gmapsdata/jobs.db
gmapsdata/jobs.db-shm
gmapsdata/jobs.db-wal
gmapsdata/*.csv

# Local browser/MCP caches
.playwright-mcp/
skills-lock.json

# Editor-local settings
.vscode/

# Nested repos (cloned here for convenience, pushed to their own remotes)
stl-knights/
